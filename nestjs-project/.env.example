# Application
NODE_ENV=development
PORT=3000

# Database (use Docker Compose service name as host when running inside containers)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=streamtube
DB_PASSWORD=streamtube
DB_NAME=streamtube
